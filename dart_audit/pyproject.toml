[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "dart-audit"
version = "0.6.0"
description = "Agentic-DART — tamper-evident JSONL audit logger"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
