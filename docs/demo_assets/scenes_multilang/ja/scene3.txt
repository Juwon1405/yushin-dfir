多くのエージェント型DFIRツールは。実はシステムプロンプトに過ぎません。LLMに大人しく振る舞うよう、お願いしているだけです。

私たちは、誤った振る舞いをできる能力そのものを取り除きました。

バイパステストスイートは検証します。破壊的な操作。execute shell。write file。mount。eval。これらがすべて登録されていないことを。

そして、evidence rootに対するpath traversal の試みが。MCP境界そのもので遮断されることを。

SIFTアダプターテストスイートは。同様の保証が25個すべてのsubprocess wrapperに対して成立することを確認します。

関数を追加しても、この境界は弱まりません。なぜなら、境界そのものが。canonical name setだからです。
