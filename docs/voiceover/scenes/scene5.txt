v0.6.1 ships three new native functions. parse underscore m a c o s underscore quarantine reads the LSQuarantineEvent SQLite database — Sarah Edwards's standard macOS download-provenance artefact. parse underscore linux underscore cron underscore jobs enumerates every cron path with attacker-pattern flagging — curl-pipe-shell, base64 decode, at-reboot triggers, slash temp shell scripts. And detect underscore d n s underscore tunnelling — shown here — opens MITRE tactic TA zero zero one one, Command and Control, via Shannon entropy, query volume, and Iodine and dnscat2 signature detection.
